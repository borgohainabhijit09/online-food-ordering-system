
## UI Notifications & Confirmations
Never use the browser-native window.alert or window.confirm functions for notifying users or asking for confirmation. Always use internal UI components (like a custom Modal component for confirmations, and eact-hot-toast or similar for toast notifications) to ensure a consistent, internal tool experience.
